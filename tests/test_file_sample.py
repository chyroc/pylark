# Code generated by lark_sdk_gen. DO NOT EDIT.

from pylark.log import logger
from tests.test_conf import app_all_permission
import unittest
import pylark
import pytest


class Test_File_Sample_RequestFailed(unittest.TestCase):
    def __init__(self):
        super().__init__()
        self.cli = app_all_permission.ins()
        # self.cli.mock().MockGetTenantAccessToken(mockGetTenantAccessTokenFailed)
        # self.cli.mock().MockGetAppAccessToken(mockGetTenantAccessTokenFailed)
        self.module_cli = self.cli.file

    def test_request_failed_upload_image(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.UploadImage(pylark.UploadImageReq())
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_download_image(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.DownloadImage(pylark.DownloadImageReq())
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_upload_file(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.UploadFile(pylark.UploadFileReq())
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_download_file(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.DownloadFile(pylark.DownloadFileReq())
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

# Code generated by lark_sdk_gen. DO NOT EDIT.

import unittest
import pylark
import pytest
from tests.test_conf import app_all_permission, app_no_permission
from tests.test_helper import mock_get_tenant_access_token_failed


def mock(*args, **kwargs):
    raise pylark.PyLarkError(scope="scope", func="func", code=1, msg="mock-failed")


def mock_raw_request(*args, **kwargs):
    raise pylark.PyLarkError(
        scope="scope", func="func", code=1, msg="mock-raw-request-failed"
    )


# mock get token
class TestFileSampleMockGetTokenFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestFileSampleMockGetTokenFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.cli.auth.get_tenant_access_token = mock_get_tenant_access_token_failed
        self.cli.auth.get_app_access_token = mock_get_tenant_access_token_failed
        self.module_cli = self.cli.file

    def test_mock_get_token_upload_image(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_image(pylark.UploadImageReq())

        assert "msg=failed" in f"{e}"

    def test_mock_get_token_download_image(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_image(pylark.DownloadImageReq())

        assert "msg=failed" in f"{e}"

    def test_mock_get_token_upload_file(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_file(pylark.UploadFileReq())

        assert "msg=failed" in f"{e}"

    def test_mock_get_token_download_file(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_file(pylark.DownloadFileReq())

        assert "msg=failed" in f"{e}"


# mock mock self func
class TestFileSampleMockSelfFuncFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestFileSampleMockSelfFuncFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.module_cli = self.cli.file

    def test_mock_self_func_upload_image(self):
        origin_func = self.module_cli.upload_image
        self.module_cli.upload_image = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_image(pylark.UploadImageReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.upload_image = origin_func

    def test_mock_self_func_download_image(self):
        origin_func = self.module_cli.download_image
        self.module_cli.download_image = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_image(pylark.DownloadImageReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.download_image = origin_func

    def test_mock_self_func_upload_file(self):
        origin_func = self.module_cli.upload_file
        self.module_cli.upload_file = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_file(pylark.UploadFileReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.upload_file = origin_func

    def test_mock_self_func_download_file(self):
        origin_func = self.module_cli.download_file
        self.module_cli.download_file = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_file(pylark.DownloadFileReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.download_file = origin_func


# mock raw request
class TestFileSampleMockRawRequestFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestFileSampleMockRawRequestFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.module_cli = self.cli.file
        self.cli.raw_request = mock_raw_request

    def test_mock_raw_request_upload_image(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_image(pylark.UploadImageReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg

    def test_mock_raw_request_download_image(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_image(
                pylark.DownloadImageReq(
                    image_key="x",
                )
            )

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg

    def test_mock_raw_request_upload_file(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_file(pylark.UploadFileReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg

    def test_mock_raw_request_download_file(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_file(
                pylark.DownloadFileReq(
                    file_key="x",
                )
            )

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg


# real request
class TestFileSampleRealRequestFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestFileSampleRealRequestFailed, self).__init__(*args, **kwargs)
        self.cli = app_no_permission.ins()
        self.module_cli = self.cli.file

    def test_real_request_upload_image(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_image(pylark.UploadImageReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

    def test_real_request_download_image(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_image(
                pylark.DownloadImageReq(
                    image_key="x",
                )
            )

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

    def test_real_request_upload_file(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.upload_file(pylark.UploadFileReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

    def test_real_request_download_file(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_file(
                pylark.DownloadFileReq(
                    file_key="x",
                )
            )

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

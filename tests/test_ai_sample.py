# Code generated by lark_sdk_gen. DO NOT EDIT.

from pylark.log import logger
from tests.test_conf import app_all_permission
import unittest
import pylark
import pytest


class Test_AI_Sample_RequestFailed(unittest.TestCase):
    def __init__(self):
        super().__init__()
        self.cli = app_all_permission.ins()
        # self.cli.mock().MockGetTenantAccessToken(mockGetTenantAccessTokenFailed)
        # self.cli.mock().MockGetAppAccessToken(mockGetTenantAccessTokenFailed)
        self.module_cli = self.cli.ai

    def test_request_failed_recognize_basic_image(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.RecognizeBasicImage(
                pylark.RecognizeBasicImageReq()
            )
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_recognize_speech_stream(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.RecognizeSpeechStream(
                pylark.RecognizeSpeechStreamReq()
            )
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_recognize_speech_file(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.RecognizeSpeechFile(
                pylark.RecognizeSpeechFileReq()
            )
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_translate_text(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.TranslateText(pylark.TranslateTextReq())
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_detect_text_language(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.DetectTextLanguage(
                pylark.DetectTextLanguageReq()
            )
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

    def test_request_failed_detect_face_attributes(self):
        with pytest.raises(ZeroDivisionError) as e:
            res, response = self.module_cli.DetectFaceAttributes(
                pylark.DetectFaceAttributesReq()
            )
            print("e", e)

            # as.NotNil(err)
            # as.Equal(err.Error(), "failed")

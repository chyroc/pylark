# Code generated by lark_sdk_gen. DO NOT EDIT.

import unittest
import pylark
import pytest
from tests.test_conf import app_all_permission, app_no_permission
from tests.test_helper import mock_get_tenant_access_token_failed


def mock(*args, **kwargs):
    raise pylark.PyLarkError(scope="scope", func="func", code=1, msg="mock-failed")


def mock_raw_request(*args, **kwargs):
    raise pylark.PyLarkError(
        scope="scope", func="func", code=1, msg="mock-raw-request-failed"
    )


# mock get token
class TestBotSampleMockGetTokenFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestBotSampleMockGetTokenFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.cli.auth.get_tenant_access_token = mock_get_tenant_access_token_failed
        self.cli.auth.get_app_access_token = mock_get_tenant_access_token_failed
        self.module_cli = self.cli.bot

    def test_mock_get_token_get_bot_info(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_bot_info(pylark.GetBotInfoReq())

        assert "msg=failed" in f"{e}"

    def test_mock_get_token_add_bot_to_chat(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.add_bot_to_chat(pylark.AddBotToChatReq())

        assert "msg=failed" in f"{e}"


# mock mock self func
class TestBotSampleMockSelfFuncFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestBotSampleMockSelfFuncFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.module_cli = self.cli.bot

    def test_mock_self_func_get_bot_info(self):
        origin_func = self.module_cli.get_bot_info
        self.module_cli.get_bot_info = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_bot_info(pylark.GetBotInfoReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.get_bot_info = origin_func

    def test_mock_self_func_add_bot_to_chat(self):
        origin_func = self.module_cli.add_bot_to_chat
        self.module_cli.add_bot_to_chat = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.add_bot_to_chat(pylark.AddBotToChatReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.add_bot_to_chat = origin_func


# mock raw request
class TestBotSampleMockRawRequestFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestBotSampleMockRawRequestFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.module_cli = self.cli.bot
        self.cli.raw_request = mock_raw_request

    def test_mock_raw_request_get_bot_info(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_bot_info(pylark.GetBotInfoReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg

    def test_mock_raw_request_add_bot_to_chat(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.add_bot_to_chat(pylark.AddBotToChatReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg


# real request
class TestBotSampleRealRequestFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestBotSampleRealRequestFailed, self).__init__(*args, **kwargs)
        self.cli = app_no_permission.ins()
        self.module_cli = self.cli.bot

    def test_real_request_get_bot_info(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_bot_info(pylark.GetBotInfoReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

    def test_real_request_add_bot_to_chat(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.add_bot_to_chat(pylark.AddBotToChatReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

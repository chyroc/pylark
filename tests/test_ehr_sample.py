# Code generated by lark_sdk_gen. DO NOT EDIT.

import unittest
import pylark
import pytest
from tests.test_conf import app_all_permission, app_no_permission
from tests.test_helper import mock_get_tenant_access_token_failed


def mock(*args, **kwargs):
    raise pylark.PyLarkError(scope="scope", func="func", code=1, msg="mock-failed")


def mock_raw_request(*args, **kwargs):
    raise pylark.PyLarkError(
        scope="scope", func="func", code=1, msg="mock-raw-request-failed"
    )


# mock get token
class TestEHRSampleMockGetTokenFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestEHRSampleMockGetTokenFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.cli.auth.get_tenant_access_token = mock_get_tenant_access_token_failed
        self.cli.auth.get_app_access_token = mock_get_tenant_access_token_failed
        self.module_cli = self.cli.ehr

    def test_mock_get_token_get_ehr_employee_list(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_ehr_employee_list(pylark.GetEHREmployeeListReq())

        assert "msg=failed" in f"{e}"

    def test_mock_get_token_download_ehr_attachments(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_ehr_attachments(pylark.DownloadEHRAttachmentsReq())

        assert "msg=failed" in f"{e}"


# mock mock self func
class TestEHRSampleMockSelfFuncFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestEHRSampleMockSelfFuncFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.module_cli = self.cli.ehr

    def test_mock_self_func_get_ehr_employee_list(self):
        origin_func = self.module_cli.get_ehr_employee_list
        self.module_cli.get_ehr_employee_list = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_ehr_employee_list(pylark.GetEHREmployeeListReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.get_ehr_employee_list = origin_func

    def test_mock_self_func_download_ehr_attachments(self):
        origin_func = self.module_cli.download_ehr_attachments
        self.module_cli.download_ehr_attachments = mock

        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_ehr_attachments(pylark.DownloadEHRAttachmentsReq())

        assert "msg=mock-failed" in f"{e}"
        self.module_cli.download_ehr_attachments = origin_func


# mock raw request
class TestEHRSampleMockRawRequestFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestEHRSampleMockRawRequestFailed, self).__init__(*args, **kwargs)
        self.cli = app_all_permission.ins()
        self.module_cli = self.cli.ehr
        self.cli.raw_request = mock_raw_request

    def test_mock_raw_request_get_ehr_employee_list(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_ehr_employee_list(pylark.GetEHREmployeeListReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg

    def test_mock_raw_request_download_ehr_attachments(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_ehr_attachments(
                pylark.DownloadEHRAttachmentsReq(
                    token="x",
                )
            )

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0
        assert "mock-raw-request-failed" in e.value.msg


# real request
class TestEHRSampleRealRequestFailed(unittest.TestCase):
    def __init__(self, *args, **kwargs):
        super(TestEHRSampleRealRequestFailed, self).__init__(*args, **kwargs)
        self.cli = app_no_permission.ins()
        self.module_cli = self.cli.ehr

    def test_real_request_get_ehr_employee_list(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.get_ehr_employee_list(pylark.GetEHREmployeeListReq())

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

    def test_real_request_download_ehr_attachments(self):
        with pytest.raises(pylark.PyLarkError) as e:
            self.module_cli.download_ehr_attachments(
                pylark.DownloadEHRAttachmentsReq(
                    token="x",
                )
            )

        assert e.type is pylark.PyLarkError
        assert e.value.code > 0

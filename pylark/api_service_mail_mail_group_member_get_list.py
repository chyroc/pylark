# Code generated by lark_sdk_gen. DO NOT EDIT.

from pylark.lark_request import RawRequestReq, _new_method_option
import attr
import typing
import io


@attr.s
class GetMailGroupMemberListReqDepartmentIDType(object):
    pass


@attr.s
class GetMailGroupMemberListReqUserIDType(object):
    pass


@attr.s
class GetMailGroupMemberListReq(object):
    user_id_type: GetMailGroupMemberListReqUserIDType = attr.ib(
        default=None, metadata={"req_type": "query"}
    )  # 用户 ID 类型, 示例值："open_id", 可选值有: `open_id`：用户的 open id, `union_id`：用户的 union id, `user_id`：用户的 user id, 默认值: `open_id`, 当值为 `user_id`, 字段权限要求: 获取用户 userid
    department_id_type: GetMailGroupMemberListReqDepartmentIDType = attr.ib(
        default=None, metadata={"req_type": "query"}
    )  # 此次调用中使用的部门ID的类型, 示例值："open_department_id", 可选值有: `department_id`：以自定义department_id来标识部门, `open_department_id`：以open_department_id来标识部门
    page_token: str = attr.ib(
        default="", metadata={"req_type": "query"}
    )  # 分页标记，第一次请求不填，表示从头开始遍历；分页查询结果还有更多项时会同时返回新的 page_token，下次遍历可采用该 page_token 获取查询结果, 示例值："xxx"
    page_size: int = attr.ib(
        default=0, metadata={"req_type": "query"}
    )  # 分页大小, 示例值：10, 最大值：`200`
    mailgroup_id: str = attr.ib(
        default="", metadata={"req_type": "path"}
    )  # The unique ID or email address of a mail group, 示例值："xxxxxxxxxxxxxxx or test_mail_group@xxx.xx"


@attr.s
class GetMailGroupMemberListRespItemType(object):
    pass


@attr.s
class GetMailGroupMemberListRespItem(object):
    member_id: str = attr.ib(default="", metadata={"req_type": "json"})  # 邮件组内成员唯一标识
    email: str = attr.ib(
        default="", metadata={"req_type": "json"}
    )  # 成员邮箱地址（当成员类型是EXTERNAL_USER/MAIL_GROUP/OTHER_MEMBER时有值）
    user_id: str = attr.ib(
        default="", metadata={"req_type": "json"}
    )  # 租户内用户的唯一标识（当成员类型是USER时有值）
    department_id: str = attr.ib(
        default="", metadata={"req_type": "json"}
    )  # 租户内部门的唯一标识（当成员类型是DEPARTMENT时有值）
    type: GetMailGroupMemberListRespItemType = attr.ib(
        factory=lambda: GetMailGroupMemberListRespItemType(),
        metadata={"req_type": "json"},
    )  # 成员类型, 可选值有: `USER`：内部用户, `DEPARTMENT`：部门, `COMPANY`：全员, `EXTERNAL_USER`：外部用户, `MAIL_GROUP`：邮件组, `OTHER_MEMBER`：内部成员


@attr.s
class GetMailGroupMemberListResp(object):
    has_more: bool = attr.ib(
        factory=lambda: bool(), metadata={"req_type": "json"}
    )  # 是否还有更多项
    page_token: str = attr.ib(
        default="", metadata={"req_type": "json"}
    )  # 分页标记，当 has_more 为 true 时，会同时返回新的 page_token，否则不返回 page_token
    items: typing.List[GetMailGroupMemberListRespItem] = attr.ib(
        factory=lambda: [], metadata={"req_type": "json"}
    )  # 邮件组成员列表


def _gen_get_mail_group_member_list_req(request, options) -> RawRequestReq:
    return RawRequestReq(
        dataclass=GetMailGroupMemberListResp,
        scope="Mail",
        api="GetMailGroupMemberList",
        method="GET",
        url="https://open.feishu.cn/open-apis/mail/v1/mailgroups/:mailgroup_id/members",
        body=request,
        method_option=_new_method_option(options),
        need_tenant_access_token=True,
    )
